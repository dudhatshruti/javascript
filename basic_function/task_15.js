// NO ARGUMENT WITH RETURN

function one()
{
    let a=49,b=15,c;
    c = a*b;
    return c;
}
let c;
if(c % 21 == 0)
    {
            console.log("divisible",one());  
    }
    else{
        console.log("not divisible",one());  
    }

// console.log(one());